name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!"
      - name: Print another greeting
        run: |
          echo "Second Greeting"
          echo "Third Greeting"
      - name: Print goodbye
        run: echo "Done - bye!"
